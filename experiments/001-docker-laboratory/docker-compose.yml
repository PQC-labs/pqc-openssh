services:

  server:
    build:
      context: ./server
    image: pqc_lab001-openssh-server:latest
    container_name: pqc_lab001-openssh-server
    hostname: server
    networks:
      - pqc-net
    volumes:
      - ../../docker/keys:/keys:ro
    ports:
      - "2222:22"
    restart: unless-stopped

  client:
    build:
      context: ./client

    image: pqc_lab001-openssh-client:latest
    container_name: pqc_lab001-openssh-client
    hostname: client

    networks:
      - pqc-net

    volumes:
      - ../../docker/keys:/keys:ro

    depends_on:
      - server

    stdin_open: true
    tty: true

    restart: unless-stopped

networks:
  pqc-net:
    driver: bridge
