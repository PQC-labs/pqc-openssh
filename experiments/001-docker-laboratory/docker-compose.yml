services:

  server:
    build:
      context: ./server
    container_name: pqc-server
    hostname: server
    networks:
      - pqc-net
    volumes:
      - ../../docker/keys:/keys:ro
    ports:
      - "2222:22"
    restart: unless-stopped

  client:
    build:
      context: ./client

    container_name: pqc-client
    hostname: client

    networks:
      - pqc-net

    volumes:
      - ../../docker/keys:/keys:ro

    depends_on:
      - server

    stdin_open: true
    tty: true

    restart: unless-stopped

networks:
  pqc-net:
    driver: bridge
